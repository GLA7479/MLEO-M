# Browser console checks

בדיקות בסיס בדפדפן:

- לפתוח את האתר
- לפתוח DevTools
- לעבור ל-Console
- לבדוק אם יש:
  - CSRF_INVALID
  - 403 Forbidden
  - BASE presence push rejected
  - device init failures
  - failed fetch calls

אם יש שגיאות:
- לעשות Hard Reload
- לבדוק שוב
- לעבור ל-Network

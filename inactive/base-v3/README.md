# BASE V3 (inactive)

This folder stores the archived BASE V3 implementation.

- Route removed from active pages.
- Access button removed from `pages/mining.js`.
- Source files moved from active `pages/` and `game/` paths.

If you want to reactivate in the future, move files back to active paths and restore the mining link.
